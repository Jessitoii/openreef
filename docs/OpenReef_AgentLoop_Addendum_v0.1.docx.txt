🦞OPENREEFAddendum E: Agent Loop — Query Engine İyileştirmeleri  ·  v0.1Claude Code Leak'ten Öğrenilenler — 3 Kritik EklemeBu addendum, Claude Code kaynak kodu sızıntısı (31 Mart 2026) analizindençıkan 3 kritik agent loop iyileştirmesini belgeler.Hedef dosya: lib/agent/agent_loop.dartİlgili bölüm: Tools_ACP_Context_v0.2.docx — Bölüm E.3 (Tool Execution Loop)Claude Code'un kendi loop'unda tespit edilen sorunlar:  • %5.4 orphaned tool call (sonuç sessizce düşüyor)  • Sonsuz döngü riski — context sıkıştırma başarısız olduğunda  • Tehlikeli tool'lar için koordinatör onayı eksikMevcut AgentLoop (E.3 — değişmeden önce)Tools_ACP_Context_v0.2.docx, Bölüm E.3'teki mevcut loop şu şekilde:// lib/agent/agent_loop.dart — MEVCUT (v0.2)AgentLoop.run(userMessage):  context = contextEngine.assemble(...)  response = litert.generate(context)   // streaming  while response.hasToolCall:    toolCall = response.extractToolCall()    result = await toolRouter.dispatch(toolCall)    context.appendToolResult(toolCall.id, result)    response = litert.generate(context)  // continue  // Final text response  contextEngine.afterTurn(turn)  return response.textBu loop 3 sorunu barındırıyor:1. Tool çağrısı başarısız olduğunda ne olacağı tanımsız — döngü sonsuza gidebilir.2. Context limiti dolduğunda loop patlıyor, graceful sıkıştırma yok.3. Tehlikeli tool'lar (sms_send, phone_call) doğrudan çalışıyor, koordinatör onayı geçmiyor.Ekleme 1 — Circuit Breaker▸ Nereye: agent_loop.dart, while döngüsü içiClaude Code dersi: 3 ardışık sıkıştırma başarısızlığında sistem durdu.OpenReef'te LiteRT hata verirse döngü sonsuza gider → pil felaketi.Çözüm: ardışık hata sayacı + maksimum deneme sınırı.· Değişiklik: while döngüsüne hata sayacı ve guard ekle// lib/agent/agent_loop.dart — SONRASIAgentLoop.run(userMessage):  context = contextEngine.assemble(...)  response = litert.generate(context)  int _consecutiveErrors = 0;                     // ★ YENİ  const int _maxConsecutiveErrors = 3;            // ★ YENİ  while response.hasToolCall:    toolCall = response.extractToolCall()    // ★ YENİ: Circuit breaker guard    if (_consecutiveErrors >= _maxConsecutiveErrors) {      await _freezeSession('circuit_breaker_triggered');      await notify.send(                           // ★ YENİ        title: 'OpenReef — Oturum Donduruldu',        body:  '3 ardışık hata. Devam etmek için dokunun.',      );      return SessionResult.frozen;                 // ★ YENİ    }    try {      result = await toolRouter.dispatch(toolCall)      context.appendToolResult(toolCall.id, result)      _consecutiveErrors = 0;                     // ★ YENİ: başarıda sıfırla    } catch (e) {      _consecutiveErrors++;                       // ★ YENİ: hatada artır      context.appendToolError(toolCall.id, e);    }    response = litert.generate(context)  contextEngine.afterTurn(turn)  return response.textParametreDeğerNotlar_maxConsecutiveErrors3Claude Code'da da 3 — test edilmiş değer_freezeSession()oturumu dondurKonuşma geçmişi korunur, loop dururnotify.send()kullanıcıya bildirnotify tool zaten MVP'de varSessionResult.frozenyeni enum değeriUI'da 'Donduruldu' badge'i gösterilirEkleme 2 — 3-Katmanlı Context Sıkıştırma▸ Nereye: agent_loop.dart içi + context/compactor.dartClaude Code dersi: MicroCompact / AutoCompact / FullCompactMevcut OpenReef: tek katman compaction, sadece /compact komutuylaveya eşik aşılınca tetikleniyor (C.3).Eksik: loop'un her turunda token bütçesini kontrol eden proaktif katman.· Mevcut durum (Tools_ACP_Context_v0.2 — C.3)Mevcut compaction strategy şu şekilde çalışıyor:// MEVCUT — tek tetikleyici:Compaction trigger: estimatedTokens > (modelMaxTokens * 0.85)// → tüm eski turları özetle → son 8 turu koru· Sonrası: 3 katman — loop'a entegre// lib/agent/agent_loop.dart içine entegre (while döngüsü başında)while response.hasToolCall:  // ★ YENİ: Her turda token bütçesini kontrol et  final budget = contextEngine.estimateTokens(context);  // KATMAN 1 — MicroCompact (API çağrısı yok, <5ms)  // Tetik: eski tool result'lar 5 turdan önce  if (budget.oldToolResults > 0) {    context = compactor.microCompact(context);  // eski çıktıları kırp  }  // KATMAN 2 — AutoCompact (API çağrısı var, asenkron)  // Tetik: pencere doluluk oranı > %80  if (budget.ratio > 0.80) {    context = await compactor.autoCompact(      context,      reserveTokens: 2000,     // her zaman bu kadar boş tut      maxSummaryTokens: 4000,  // özet en fazla bu kadar    );  }  // KATMAN 3 — FullCompact (/compact komutu veya kritik eşik)  // Tetik: kalan < 1500 token VEYA kullanıcı /compact yazdı  if (budget.remaining < 1500 || session.compactRequested) {    context = await compactor.fullCompact(      context,      reInjectRecentFiles: true,   // son 3 dosyayı yeniden ekle      reInjectActiveSkills: true,  // aktif skill içeriklerini koru    );    session.compactRequested = false;  }  // ... tool call devam eder· context/compactor.dart — yeni metodlar// lib/context/compactor.dart — mevcut compact() metoduna ekclass ReefCompactor {  // ★ YENİ — Katman 1: Yerel kırpma, API yok  AssembleResult microCompact(AssembleResult ctx) {    // 5 turdan önce tool result varsa kırp    return ctx.copyWith(      messages: ctx.messages.map((m) {        if (m.isToolResult && m.turnsAgo > 5) {          return m.copyWith(content: '[tool result pruned]');        }        return m;      }).toList(),    );  }  // ★ YENİ — Katman 2: Otomatik özet, API çağrısı var  Future<AssembleResult> autoCompact(AssembleResult ctx, {    required int reserveTokens,    required int maxSummaryTokens,  }) async {    // Eski turları özetle, yeni turları koru    final split = _splitHistory(ctx, keepRecentTurns: 8);    final summary = await llmTask.summarize(      split.old,      maxTokens: maxSummaryTokens,    );    return ctx.copyWith(      messages: [...summary.asMessages(), ...split.recent],    );  }  // Mevcut compact() → fullCompact() olarak yeniden adlandırıldı  // + reInjectRecentFiles ve reInjectActiveSkills parametreleri eklendi  Future<AssembleResult> fullCompact(AssembleResult ctx, {    bool reInjectRecentFiles = false,    bool reInjectActiveSkills = false,  }) async {    // ... mevcut logic ...    // + dosya/skill yeniden enjeksiyonu  }}KatmanTetikleyiciAPI Çağrısı?MaliyetMicroCompactEski tool result (> 5 tur)Hayır< 5ms, pil dostuAutoCompactBudget > %80 dolduEvet (llm_task)~500ms, asenkronFullCompact< 1500 tok kaldı / /compactEvet (llm_task)~1-2s, nadirEkleme 3 — Mailbox Pattern (Koordinatör Onayı)▸ Nereye: agent/tool_router.dart + yeni agent/mailbox.dartClaude Code dersi: worker agent tehlikeli operasyonu bağımsız onaylayamaz.Koordinatörün mailbox'ına istek gönderir, koordinatör değerlendirir.Atomic claim: iki worker aynı onayı eş zamanlı işleyemez.OpenReef'te mevcut: requiresConfirmation flag'i tool manifest'te var (G.1).Eksik: sub-agent'lar bu flag'i atlayabiliyor — tool_router direkt dispatch ediyor.· Mevcut tool_router.dart — sorun// MEVCUT — sub-agent için de aynı path:Future toolRouter.dispatch(ToolCall call) {  if (tool.requiresConfirmation && session.isMainAgent) {    await userConfirm(call);  // ← sadece main agent kontrol ediyor  }  return tool.execute(call); // ← sub-agent direkt çalıştırıyor!}· Sonrası: ToolRouter + Mailbox// lib/agent/mailbox.dart — YENİ DOSYAclass AgentMailbox {  // Her oturum için bir mailbox  // Sub-agent → coordinator, coordinator → onay/ret  final _pending = <String, Completer<MailboxDecision>>{};  // Sub-agent buraya istek gönderir  Future<MailboxDecision> requestApproval({    required String workerSessionKey,    required ToolCall call,  }) async {    final requestId = _generateId();    final completer = Completer<MailboxDecision>();    // Atomic claim: aynı requestId ikinci kez alınamaz    if (_pending.containsKey(requestId)) {      return MailboxDecision.rejected('already_claimed');    }    _pending[requestId] = completer;    // Koordinatöre (main agent) bildir    eventBus.emit(ApprovalRequestEvent(      requestId: requestId,      workerKey: workerSessionKey,      toolCall: call,    ));    return completer.future; // koordinatörden yanıt bekle  }  // Koordinatör (main agent veya UI) buraya yanıt gönderir  void resolve(String requestId, MailboxDecision decision) {    _pending[requestId]?.complete(decision);    _pending.remove(requestId);  }}// ──────────────────────────────────────────────────────// lib/agent/tool_router.dart — GÜNCELLENMİŞ dispatch()Future toolRouter.dispatch(ToolCall call, {  required String sessionKey,        // ★ YENİ parametre}) async {  final tool = registry.get(call.toolId);  if (tool.requiresConfirmation) {    if (sessionKey == 'agent:main') {      // Ana agent: kullanıcıya doğrudan sor (mevcut davranış)      final ok = await userConfirm(call);      if (!ok) return ToolResult.rejected;    } else {      // ★ YENİ: Sub-agent → mailbox'a gönder, koordinatörden bekle      final decision = await mailbox.requestApproval(        workerSessionKey: sessionKey,        call: call,      );      if (decision == MailboxDecision.rejected) {        return ToolResult.rejected;      }    }  }  return tool.execute(call);}· Hangi tool'lar mailbox'tan geçiyor?ToolrequiresConfirmationSub-agent davranışı (YENİ)phone_callEvetMailbox → main agent → kullanıcıya sorsms_sendEvetMailbox → main agent → kullanıcıya sorcontact_createEvetMailbox → main agent → kullanıcıya sorwifi_toggleEvetMailbox → main agent → kullanıcıya sorscreen_lockEvetMailbox → main agent → kullanıcıya sorclipboard_readEvetMailbox → main agent → kullanıcıya sorweb_searchHayırDoğrudan çalışır (değişmez)memory_saveHayırDoğrudan çalışır (değişmez)Birleşik: Güncellenmiş AgentLoop▸ Nereye: Tools_ACP_Context_v0.2.docx — Bölüm E.3 YERİNEBu bölüm, E.3'teki mevcut Tool Execution Loop'u tamamen aşağıdakiyle değiştirir:// lib/agent/agent_loop.dart — v0.3 (3 ekleme dahil)AgentLoop.run(userMessage, {required String sessionKey}):  context = contextEngine.assemble(...)  response = litert.generate(context)  int _consecutiveErrors = 0;  const int _MAX_ERRORS   = 3;        // [1] Circuit Breaker  while response.hasToolCall:    // ─── [1] Circuit Breaker ──────────────────────────────    if (_consecutiveErrors >= _MAX_ERRORS) {      await _freezeSession('circuit_breaker');      await notify.send(        title: 'OpenReef — Oturum Donduruldu',        body:  '3 ardışık hata oluştu. Devam etmek için dokunun.',      );      return SessionResult.frozen;    }    // ─── [2] 3-Layer Compaction ───────────────────────────    final budget = contextEngine.estimateTokens(context);    if (budget.oldToolResults > 0)      context = compactor.microCompact(context);    if (budget.ratio > 0.80)      context = await compactor.autoCompact(context,        reserveTokens: 2000, maxSummaryTokens: 4000);    if (budget.remaining < 1500 || session.compactRequested)      context = await compactor.fullCompact(context,        reInjectRecentFiles: true, reInjectActiveSkills: true);    // ─── [3] Mailbox Pattern ──────────────────────────────    toolCall = response.extractToolCall()    try {      result = await toolRouter.dispatch(        toolCall,        sessionKey: sessionKey,    // sub-agent mailbox routing      );      context.appendToolResult(toolCall.id, result)      _consecutiveErrors = 0;     // başarıda sıfırla    } catch (e) {      _consecutiveErrors++;      context.appendToolError(toolCall.id, e);    }    response = litert.generate(context)  contextEngine.afterTurn(turn)  return response.textÖzet: Hangi Dosyalar Değişiyor?DosyaDeğişiklik TürüEklemelib/agent/agent_loop.dartGüncellemeCircuit breaker + compaction guard + mailbox dispatchlib/context/compactor.dartGüncellememicroCompact() ve autoCompact() metodları eklendilib/agent/mailbox.dartYeni dosyaAgentMailbox sınıfı — sub-agent onay zincirilib/agent/tool_router.dartGüncellemedispatch() sessionKey parametresi + mailbox routinglib/agent/session_result.dartGüncellemeSessionResult.frozen enum değeri eklendiDokümanBölümDeğişiklikTools_ACP_Context_v0.2.docxE.3 — Tool Execution LoopYukarıdaki birleşik loop ile değiştirTools_ACP_Context_v0.2.docxC.3 — Compaction Strategy3-katman tablosunu ve tetikleyicileri ekleTools_ACP_Context_v0.2.docxB.4 — Spawn ConfigurationsessionKey parametresini belirtOpenReef_Master_v0.3.docxBölüm 02 — Proje Dizin Yapısımailbox.dart'ı ekle